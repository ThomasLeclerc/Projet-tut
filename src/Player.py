import pygame, sys

class player:
    best_score = getBestScore()
	score = 0
	def __init__(self,name):
		self.name = name
    
	def raiseScore(nombre):
		score += nombre

	def beatBestScore():
		if score > best_score:
			# afficher

	def getBestScore():
		#TODO
		
